# ====== CORE ML STACK (CPU-only) ======
# Use CPU-only builds to avoid CUDA DLL errors
torch==2.3.0+cpu
torchvision==0.18.0+cpu
torchaudio==2.3.0+cpu
# The line below ensures pip fetches CPU wheels from PyTorch’s repo
--extra-index-url https://download.pytorch.org/whl/cpu

# ====== NLP & MODELING ======
transformers==4.30.2
scikit-learn==1.5.2
pandas>=2.0.0
numpy>=1.24.0

# ====== WEB APP & API ======
fastapi==0.95.2
uvicorn==0.22.0
python-dotenv
httpx

# ====== EXPERIMENT TRACKING & TESTING ======
mlflow==2.5.0
prometheus-client
pytest
